---
applications:
  .:
    name: violination
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mongodb-e4bd9:
        type: mongodb
